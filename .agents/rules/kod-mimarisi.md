---
trigger: always_on
---

Projede MVC kod mimarisini kullanarak kod yazıyoruz. Yapılan tüm düzenlemeleri bu yapıya uygun şekilde yapmalıyız. Router->Middlewares->Controller->Validator->DTO->Policies->Services->Repository->Model yapısını korumalıyız. Kullanılan Enum dosyalarına her zaman dikkat etmeliyiz. Merkesi hata yakalama ve loglama sistemine de uymalıyız.

inline namespace kullanma